# steveX 已实现内容清单

> 基于对 `steveX/` 全部源码（Node 大脑 + 两个 Fabric mod + 脚本 + 前端）的完整通读整理。
> 整理日期：2026-08-29。当前工作区为**未提交的 mod 透传改造版**（旧 mineflayer 命令层 `src/commands/` 已删除，共 102 处未提交改动）。
>
> **v2.32（2026-09-06）修订**：视觉链路携带世界类型（维度）——采集端各视觉 store 写按维分桶文件
> （顶层 `currentDimension` + `worlds` 桶）、`snapshot` 响应带 `dimension`、cells 反通道带维标签；
> 记忆世界**镜像跟随** agent 所在维（活动维 = terrain 顶层 `currentDimension`），各复原通道状态按维隔离、
> 只复原 vanilla 三维、跨维坐标永不互撞。详见 `docs/世界类型区分与镜像复原设计方案.md`。
>
> **v2.33（2026-09-06）修订**：记忆世界传送门冻结——下界门 / 末地门 / 末地折跃门变为**纯装饰**（站入不
> 传送、永不新建目的地门 / 末地平台 / 弹 credits），杜绝踏入复现门时 vanilla 在虚空维自动造门污染镜像；
> 镜像跟随跨维摆放不受影响。详见 `docs/传送门冻结设计方案.md`。
>
> **v2.36（2026-09-08）修订**：半透明减量（§7.12）——记忆世界排干的水 / 被敲掉的满格透明（玻璃块/染色
> 玻璃/冰）不再永成幽灵：反向通道 cells 文件升 version=3、按判据场分两段（opaque/main 段 = 实心不透明
> ∪ 冻结实体占用格 ∪ 岩浆；translucent 段 = 水 ∪ 满格透明）；采集侧新增 `DeletionJudge.testTranslucent`
>（translucent 深度场同构判据）并按图形配置路由（Fabulous → translucent 场 / Fancy-Fast → 并入 main 场 /
> Fabulous PBO 降级 → 空集），`ObjectResolver` 两段删格并入同一 deletions；记忆侧 `DeletionApplier` 守卫
> 放宽到 `isDeletableContent`（实心不透明 ∪ 流体 ∪ 满格透明）、新增 `removalTranslucentEnabled` 开关。
> 详见 `docs/视觉api设计方案.md` §7.12（✅ 已实现 2026-09-08，待游戏内验证）。
>
> **v2.40（2026-09-18）修订**：观察边界收紧——`vision/entity` 由"直读任意活体整份 NBT"改为查
> 落盘 store（范围自动收敛为"本帧被渲染 + 当前维"，语义由 inspect 变 recall，`collectEntityNbt`
> 与 TTL/LRU 缓存删除、`force` 参数删除）；`vision/snapshot` 掉落物 `item` 补 `enchanted`（判据 A =
> `DataComponents.ENCHANTMENTS` 非空）。详见 `docs/观察边界与物品可见性设计方案.md`。
>
> **v2.41（2026-09-19）修订**：实体属性观测面——`vision/snapshot` 的活体实体（生物/玩家）新增 7 个
> 平铺实体级键（`equipment`/`name`/`effects`/`baby`/`maxHealth`/`pose`/`onFire`），
> `entities.nbt` 条目新增 `living` 复原口径全量键，记忆侧 `EntityRestorer.applyLiving` 据此复原装备 /
> 名字牌 / 药水效果 / 幼年体型 / 姿态 / 着火。"落盘要全量、读取面要薄"的通则由此明确。四个待确认项
> 拍板：全落盘 / `maxHealth` 不落盘 / `pose` 恒写 / `effects` 不过滤 `isVisible()`（后两项为**超观测例外**）。
> 详见 `docs/实体属性观测面设计方案.md`（✅ 已实现，待游戏内验证）。
>
> ⚠️ **v2.41 补记（同日）**：上表 7 键中的 **`effects` 当前恒空**——实施中核实发现药水效果
> **根本不在客户端同步链路上**（`ServerEntity.sendPairingData` 不发效果；vanilla 只同步
> `DATA_EFFECT_PARTICLES`，即**只有颜色**，id/等级/时长全丢）⇒ 观测端 `getActiveEffects()` 对除
> 本地玩家及其乘客外的任何实体恒为空集（既有 `f3.target.entity.effects` 同样如此）。拍板为
> **「保留全部实现（读取面键 / 落盘键 / 复原路径），只把取数点置空，稍后再解决」**：取数点是
> `LivingSummary.collectEffects`，当前恒返回 `List.of()`，源头解决时只改这一处。**故
> `snapshot.entities[]` 的 `effects` 键现在不会出现、`living.effects` 不写盘**，读取面剩 6 键可用。
> 同时"不过滤 `isVisible()`"那条拍板**悬空作废**（链路给的本来就是过滤后的粒子）。见设计文档 §3.2/§8.4/§10-5。
>
> ✅ **v2.42（2026-09-19）补记——上述缺口已补上**：走**集成服务器**路线。连真实服务器时效果不上线，
> 但单机 / 自己是局域网主机时**服务端权威数据就在同一个 JVM 里**（`mc.getSingleplayerServer()`）。
> 新增 `vision/EffectSampler`（**不可变信箱**：服务端 tick 线程每 tick 采样 → 渲染线程只读；
> 直读不行——`getActiveEffects()` 返回的是活视图，跨线程迭代会 CME）+ `SteveX` 注册
> `ServerTickEvents.END_SERVER_TICK` / `ServerLifecycleEvents.SERVER_STOPPED` 两个既有事件
> （**无新增 mixin**）。取数点 `LivingSummary.collectEffects` 换实现，**签名与两处调用点一行未动**。
> `effects` 现按**过滤 `isVisible()`** 给出（没有粒子的效果看不见，恢复为边界合规、不再是超观测例外）；
> 落盘侧仍全量不过滤。`vision/snapshot` 顶层新增 **`effectsSource`**
> （`"integrated_server"` | `"unavailable"`）——**数据源是进程级事实**，连真实服务器时 `effects` 键
> 缺席意味着"未知"而非"没有效果"，必须靠它区分。`f3` 按既有拍板**不改**（`F3Api:199` 仍恒空）。
> 记忆端**零改动**（`applyLiving` 复原路径 v2.41 已就位）。详见设计文档 §12。

> 🔧 **v2.43 / v2.44 / v2.45（2026-09-19）——容器 API：两处修复 + 两处补充**（游戏内测试驱动）：
>
> **① `container/button` 此前是死调用。** 它只调了 `menu.clickMenuButton(p, btn)` 并把返回值当结果，
> 但**客户端 menu 的 `ContainerLevelAccess` 是 `NULL`**（`MenuType` 的 create 走
> `ContainerLevelAccess.NULL`，其 `evaluate` 直接返回 `Optional.empty()`），于是 `clickMenuButton`
> 里真正干活的 `this.access.execute(...)` 在客户端**空转**——只有服务端 menu 持有带 level/pos 的
> access。vanilla 的 `EnchantmentScreen` 同样只用它的返回值当"要不要发包"的闸门，真正生效的是紧随的
> `gameMode.handleInventoryButtonClick`。已补上这一步，**逐句对齐 vanilla**。
> 影响面：`clickMenuButton` 在 vanilla 里只被**附魔台 / 讲台 / 织布机 / 切石机**覆写，这四类界面此前
> 按钮全无反应——而 `container/get` 早就在读它们的选中态（`page` / `selectedPattern` / `selectedRecipe`），
> **能读不能写**，现在写的一侧补齐了。
> **`accepted` 语义同步更正**：只表示"本地预检通过且已发包"，**不是执行结果**（服务端无论成败都不回包）；
> 是否真生效必须靠随后的 `container/get` 观察（附魔台看 `goldCount` 是否被扣、`stateId` 是否变化、
> 附魔位物品是否长出 `enchantments`）。
>
> **② `container/get` 附魔台分支补 `enchantName`。** `enchantClue` 是 `holders.getId(...)` 的
> **注册表裸数字 id**（`EnchantmentMenu:98`），agent 拿 `[40,1,17]` 无从知道"第 3 个选项是什么附魔"。
> 新增 `enchantName`——用与 `EnchantmentScreen:158-161` 同一句 `lookupOrThrow(Registries.ENCHANTMENT)`
> 反查 Holder，取 **`Holder.getRegisteredName()`**（与本 API 输出附魔的既有惯例一致，见
> `InventoryApi.slotItem` 的 `enchantments`）。与 `costs`/`levelClue` **同长同序、下标一一对应**；
> 选项不可用（`enchantClue[i] == -1`）时该项为 `null`。`enchantClue` 保留（原始 id 供排查，且删字段
> 属破坏性改动）。
>
> **③ `container/get` 附魔台分支补 `bookshelves`（v2.44，有效书架数）。** 这个数 vanilla 当
> **局部变量**用（`EnchantmentMenu.slotsChanged` 里的 `int bookcases`），既不存字段也不进 DataSlot
> ⇒ **根本不上同步链路**，客户端副本没有，只能重算。但**不是复刻服务端算法**——直接用 vanilla 自己的
> 公开判定 `EnchantingTableBlock.isValidBookShelf` / `BOOKSHELF_OFFSETS`，同一句在客户端的
> `EnchantingTableBlock.animateTick` 里本来就每帧跑（算附魔粒子），读的正是客户端自己的世界副本，
> 故无"与服务端逐字节对齐"的负担。
> **坐标只能来自准星**（客户端 menu 的 access 是 `NULL`，拿不到 pos；`ContainerMemoryTracker.bind`
> 定位方块容器用的是同一手法）。附魔界面锁视角 ⇒ 实际恒命中，但**不假定它成立**：`camera/turn` 能在
> 界面开着时转动视角，那时准星已是别的方块，照算就是假事实。故 `countBookshelves` 验证准星方块
> **确实是附魔台**，否则返回 `null` ⇒ **该键不写——缺席＝未知**（与 v2.42 `effects` 同口径），
> **绝不写 0**（0 是断言"周围没书架"，而事实是"没法判定"）。返回**原始计数**；vanilla 算附魔等级时
> `getEnchantmentCost` 会 clamp 到 15，故 >15 与 15 等效。
>
> **④ `container/get` 切石机分支补 `recipes`（v2.45，方案列表的具体内容）。** 此前该分支只给两个数：
> `selectedRecipe`（下标）和 `visibleRecipes`（**数量**——命名有歧义，它是 `int` 不是列表）。
> 方案列表本身没暴露。但它**本来就在客户端**：是 `StonecutterMenu` 的 `recipesForInput` **字段**
> （不是 DataSlot），由客户端自己的 `level.recipeAccess().stonecutterRecipes().selectByInput(...)`
> 算出——vanilla 的 `StonecutterScreen:82-91` 正是从客户端 menu 读它画那 12 个按钮，并用同一句
> `optionDisplay().resolveForFirstStack(SlotDisplayContext.fromLevel(level))` 解析出图标。
> 故这不是新开同步通道，只是把已有数据接出来（与 ② 同形状）。
> 取 GUI 上显示的**图标**（`optionDisplay`）而非 `Recipe#assemble` 的结果：与所见一致，且对任何
> `SlotDisplay` 变体都成立。**下标必须与 `container/button` 的 `button` 号一一对应**，故逐个输出、
> **绝不跳过**——即使某图标解析为空（`SlotDisplay.Empty` → `count:0`）也照占一位，否则下标错位；
> 条目数应恒等于 `visibleRecipes`。`mc.level == null` 时**不写该键**（缺席＝未知，与 ③ `bookshelves`
> 及 v2.42 `effects` 同口径）——写 `[]` 会被读成"没有方案"这一断言。

> 🔧 **v2.46（2026-09-20）——容器 API：`container/get` 织布机分支补 `patterns`**（游戏内测试驱动）：
>
> **① `container/get` 织布机分支补 `patterns`（方案列表的具体内容）。** 此前该分支只给
> `selectedPattern` 一个下标，agent 无从知道"第 N 号是什么图案"，只能盲点。
> 列表本来就在客户端：`selectablePatterns` 是 `LoomMenu` 的**字段**（不是 DataSlot），由客户端
> 自己的 `slotsChanged → getSelectablePatterns()` 算出——vanilla 的 `LoomScreen:131` 正是读它
> 画那 4×4 个按钮。故与新开同步通道无关，只是把已有数据接出来（与切石机 `recipes` 同形状）。
> 图案槽为空时列表来自 `BannerPatternTags.NO_ITEM_REQUIRED`（1.21.11 实测 **32 项**）；放进图案物
> 则改由该物的 `PROVIDES_BANNER_PATTERNS` 决定。两种情况都由 vanilla 自己算。
> 下标与 `container/button` 的 `button` 号一一对应，逐个输出、**绝不跳过**。旗帜或染料缺一
> ⇒ `[]`（这是**权威事实**，见 `LoomMenu.slotsChanged`：旗帜或染料缺一即清空）；`mc.level == null`
> ⇒ **不写该键**（缺席＝未知，与 ③ 及 v2.42 `effects` 同口径）。
>
> **② 织布机的按钮号是全局下标，滚动不影响 API。** 界面 4×4 一次只显示 16 个、要滚动才看得全
> 32 个，但 `LoomScreen:193-197` 已把滚动偏移 `startRow` 折进 `index` 再发给服务端——
> `int index = (row + startRow) * 4 + column`。故 `patterns[]` 给**全量**，点第 30 个方案
> **不需要先滚动**。这是织布机在 API 形状上与切石机唯一的额外差异。
>
> **③ 图标取"选它就会得到的那面旗"，不是界面上的灰底预览旗。** 逐句对齐
> `LoomMenu.setupResultSlot`（`LoomMenu.java:263-280`）：旗帜槽那面旗 `copyWithCount(1)`，
> 再把图案层 `addAll(layers).add(pattern, dyeColor)` **追加**其后——即产物格的算式。
> 刻意**不用** `LoomScreen` 按钮上那面旗（`LoomScreen:178` 用 `DyeColor.GRAY` 填底）：灰底是
> 界面为中性预览造的，世上并不存在那样一面旗，写进读取面即成**假事实**；"选它得到什么"才是真话。
>
> **④ `InventoryApi.slotItem` 新增 `bannerPatterns` 字段（本轮的必要前提）。** 此前 `slotItem`
> 不吐 `BANNER_PATTERNS` 组件，带图案的旗帜与素旗在 API 里完全同形（同 `id`／同 `count`）——
> 而织布机的全部方案正是"同一面旗＋不同图案"，缺这段则 `patterns[]` 会退化成 32 个彼此无法
> 区分的条目，图标方案根本不成立。该字段对**任何位置**的旗帜都生效（`inventory` 也读得到）；
> 图案是持有者可见的（物品模型与 tooltip 都显示），与观察边界一致；无图案层时**不写该键**。
>
> 另需记明：织布机菜单的格子编号与切石机**不同**——`0=旗帜 1=染料 2=图案 3=产物`，
> `4–30` = 背包 9–35、`31–39` = 快捷栏 0–8（换算 `s≤8 ? 31+s : s-5`；切石机/附魔台是
> `2–28`/`29–37` 与 `s≤8 ? 29+s : s-7`）。拿切石机的编号套织布机会放错格。

> 🔧 **v2.47（2026-09-21）——容器 API：`container/get` 信标分支补 `effects` 与当前选中，出站序列化开 `serializeNulls`**（游戏内测试驱动）：
>
> **① `container/get` 信标分支补 `effects`（全部可选效果）+ `primaryEffect`/`secondaryEffect`（当前选中）。**
> 此前该分支只给一个 `levels`，agent 既不知道有哪些效果可设，也不知道当前等级下界面上哪几个能点。
>
> **② 与切石机 `recipes`／织布机 `patterns` 的关键差别：这份列表不在菜单里，也不需要服务端回读。**
> 它是静态常量 `BeaconBlockEntity.BEACON_EFFECTS`（`BeaconBlockEntity.java:53-58`，4 组 2/2/1/1
> 共 6 个），每个信标、每个存档都一样 ⇒ **不依赖 `mc.level`**，没有"缺席＝未知"这一支。
> 那 6 项是 `speed, haste` / `resistance, jump_boost` / `strength` / `regeneration`（`tier` 0–3）。
>
> **③ `canBePrimary`/`canBeSecondary` 复刻的是界面门禁，不是服务端规则。** 逐句对齐
> `BeaconScreen.java:69-101 + :216`（`active = tier < levels`）：主效果列放四组全部，某效果可点
> ⟺ `tier < levels`；副效果列**只放第 4 组**（即只有 regeneration），且 `levels ≥ 4` 才有这一列。
> **而服务端根本不过滤 tier**——`BeaconBlockEntity.filterEffect:111` 只按 `VALID_EFFECTS`（6 个的并集）
> 过滤，`applyEffects:238-259` 只看 `levels` 决定范围/时长/倍率；
> `ServerGamePacketListenerImpl.handleSetBeaconPacket:776` 拿到包就 `menu.updateEffects(...)`，无等级校验。
> ⇒ 现在的 `container/beacon` **能设出界面上点不到的效果**（如 1 级信标设 regeneration，服务端照单全收）。
> 故这两个布尔是 agent 判断"界面允不允许"的**唯一**依据，必须给。
>
> **④ 出站 Gson 开 `serializeNulls`（本轮的必要前提之二）。** vanilla 用 `0` 编码"没选效果"
> （`BeaconMenu.decodeEffect:107`），语义是**已知为空**而非未知；但 Gson 默认会**丢弃值为 null 的
> map 条目**（实测 2.13.2：`{levels:3, primaryEffect:null, after:"x"}` → `{"levels":3,"after":"x"}`，
> 键连影子都没有），于是"没选效果"会被静默降级成"未知"，与项目口径冲突。
> 新增 `AgentWebSocketServer.GSON_OUT`（`serializeNulls`）专供出站，`GSON` 仍管入站解析。
> 副作用**已核实、只有一处、且是修正**：`f3` 的 `target.block/fluid/entity` 本来就被显式 put 成
> null（`F3Api.java:145`，意图是"准星没指到"），此前一直被 Gson 吞掉；开启后真的以 `null` 出现。
> 出站序列化点全项目只有 `AgentWebSocketServer.send()` 一处；`data` 永不为 null（`result == null`
> 走 error 分支），故没有别的 key 会突然长出 `null`。
>
> 由此确立一条**与既有口径并列**的编码规则，此后新增读取面字段时二选一：
> **缺席 ＝ 未知**（客户端不知道，靠不 put 该键）；**`null` ＝ 已知为无**（vanilla 自己算出没有，靠 put null）。

---

## 0. 总体架构（已落地的样子）

```
LLM / Web 面板
   ↓ POST /api/mod/:method（结构化透传）
ModWSClient（steveX 侧 WS 客户端）
   ↓ ws://127.0.0.1:25550  {id, method, params} → {id, ok, data | error}
采集端 mod（真实世界客户端，vendor/stevex-template-1.21.11）
   ↓ 写 <gameDir>/stevex/vision/*.nbt + memory_cells.bin（反向通道）
记忆世界 mod（vendor/stevex-test-template-1.21.11，文件驱动、mtime 门控）
```

三个进程，各自独立窗口：

| 进程 | 启动 | 已实现角色 |
|---|---|---|
| 采集端（真实世界） | `npm run mc:capture` | Minecraft 客户端内 mod，WS 25550，51 方法控制 + 深度缓冲视觉采集落盘 |
| steveX（脑） | `npm start` | Express Web 面板 8090 + mod 方法透传 + WS 广播 |
| 记忆世界 | `npm run mc:memory` | 独立客户端，读 .nbt 复原"冻结的瞬间" |

---

## 1. 采集端 mod（`vendor/stevex-template-1.21.11`）

Fabric 1.21.11 客户端 mod，Java 21。**功能完整**，除两个空壳类外无未完成实现。

### 1.1 WebSocket JSON-RPC 服务（`AgentWebSocketServer.java`）
- 监听 **25550**（`java-websocket` 实现，`setReuseAddr(true)`），客户端初始化时 `launch()` 启动。
- 协议：请求 `{"id":1,"method":"player","params":{}}`；成功 `{"id":1,"ok":true,"data":{}}`；失败 `{"id":1,"ok":false,"error":"msg"}`；标量结果包一层 `{"value":…}`。
- 未知方法 → `unknown method: xxx`；解析失败 → `invalid message format`。
- **多连接支持**（不做单连接限制，handler 为静态共享注册表）。
- **busy 语义**：`busyMethods` 集合上报，`status` 方法自身不算 busy；`lastError` 静态字段记录最近错误。
- 统一跨线程通道 `runOnClient(timeoutMs, what, action)`：动作投递 MC 主线程并阻塞等待（超时只 warn 不抛）。
- 参数/格式化助手：`bool/num/int/double`、`f1/f2/f3`。

### 1.2 API 方法（51 个，15 组，与 `src/mod/methods.js` 一致）

| 分组 | 方法 | 说明 |
|---|---|---|
| **持续按键 (9)** | `key/up` `key/down` `key/left` `key/right` `key/jump` `key/sprint` `key/sneak` `key/attack` `key/use` | 参数 `{pressed:true\|false}`，按住/松开 |
| **一次性按键 (11)** | `key/attack-once` `key/use-once` `key/drop` `key/pick-item` `key/inventory` `key/swap-hands` `key/chat` `key/command` `key/perspective` `key/quick-actions` `key/hotbar` | `key/hotbar` 参数 `{slot:0-8}` |
| **玩家列表 (1)** | `tablist` | Tab 玩家（name/uuid/gamemode/latency/team/displayName）+ 计分板 objective |
| **视角 (2)** | `camera/turn` `camera/look-at` | 参数度（`dx,dy` / `x,y,z`），最短路径旋转；`AgentCamera` 底层 `turn(deg×1/0.15)` |
| **感知/查询 (5)** | `player` `inventory` `f3` `sound` `profiler` | 见下方细化 |
| **系统 (3)** | `status` `settings/get` `settings/set` | `settings` 含 renderDistance/simulationDistance/fov |
| **容器 (8)** | `container/get` `container/slot` `container/button` `container/text` `container/close` `container/drag` `container/beacon` `container/select-trade` | 支持熔炉/附魔/信标/酿造/铁砧/村民/讲台/织布机/切石机专属数据 |
| **聊天 (3)** | `chat/text` `chat/send` `chat/close` | `chat/send` 未开聊天栏时降级走 `sendCommand`/`sendChat` |
| **告示牌 (3)** | `sign/get` `sign/set` `sign/close` | 4 行读写；`close` 触发 `ServerboundSignUpdatePacket` |
| **书本 (4)** | `book/get` `book/set` `book/page` `book/close` | 支持追加页（上限 100）、翻页、保存 |
| **视觉 (2)** | `vision/snapshot` `vision/entity` | 见 1.3 |

**关键方法返回结构：**
- `player` → `vitals`(health/food/xp/armor/air)、`flags`(onGround/sprinting…)、`effects[]`
- `inventory` → 41 格（含盔甲/副手），每格含 durability/enchantments/food/attributeModifiers/tool/customModelData 等条件字段
- `f3` → `position`(xyz/block/chunk/facing/dimension)、`chunk`、`entities`、`environment`(biome/light/tps…)、`target`(准星方块/流体/实体全量；**受 3 格触及距离限制**，只能贴脸查)
- `vision/snapshot` → `entities[]`：物理态 8 字段 + 掉落物 `item{id,count?,enchanted?}` + **活体属性（v2.41，仅 LivingEntity）**`equipment{槽:{id,count?,enchanted?}}`/`name?(仅名字牌可见)`/`effects[]`(**仅 `effectsSource="integrated_server"` 时有意义**；过滤 `isVisible()`；连真实服务器时恒缺席=未知)/`baby?(仅幼年)`/`maxHealth`/`pose`/`onFire?(仅着火)` + 展示实体 `content`。注意 `equipment` 与 `f3` 的形状不同（**已拍板的已知不一致**：本面 `count?`/`enchanted?` 均条件出现，`f3` 恒给 `enchanted` 无 `count`）。`name`/`effects`/`maxHealth` 的口径细节与超观测例外见 `实体属性观测面设计方案.md` §3/§8.4
- `profiler` → F3+1 性能饼图递归树（节点 name/percentage/globalPercentage/count/children）
- `sound` → 当前播放声音列表（id/source/xyz/volume/pitch…）
- `container/get` → 按菜单类型附专属数据（熔炉进度、**附魔台 `costs`/`enchantClue`/`enchantName`/`levelClue`/`goldCount`/`bookshelves`**（v2.43 起 `enchantName` 给附魔注册名、v2.44 起 `bookshelves` 给有效书架数）、**切石机 `selectedRecipe`/`visibleRecipes`/`recipes`**（v2.45 起 `recipes` 给方案列表的具体内容，下标＝`container/button` 的 button 号）、**织布机 `selectedPattern`/`patterns`**（v2.46 起 `patterns` 给全部织布方案，下标＝button 号；每项是一面"选它就会得到"的旗，图案读该项的 `bannerPatterns`）、**信标 `levels`/`effects`/`primaryEffect`/`secondaryEffect`**（v2.47 起 `effects` 给全部可选效果，每项 `{effect,tier,canBePrimary,canBeSecondary}`，后两个布尔复刻界面门禁 `tier < levels` 与"副效果列只放 regeneration 且 levels ≥ 4"；当前没选效果时两个 `*Effect` 是 **`null`** 而非缺席）、村民交易、讲台页码等）。注意 `slots[]` **只列非空格**；`bookshelves` 是**世界状态**（靠准星定位附魔台）、`recipes`/`patterns` 依赖 `mc.level`——两者**缺席均＝未知**而非 0/空；`effects` 则是**静态常量**（不依赖 `mc.level`、不需服务端回读）
- `container/button` → **v2.43 修复**：此前只做本地预检、从不发包（详见上方 v2.43 补记），对附魔台/讲台/织布机/切石机全无反应；现已对齐 vanilla 补上 `handleInventoryButtonClick`。返回的 `accepted` 是**预检**不是结果
- `container/drag` → 单次 QuickCraft 拖拽相位包（phase 0=begin/1=add/2=finish，type 0=均分/1=每格1/2=克隆），纯原语不取物（语义见 `容器拖拽API设计方案.md`）
- `container/beacon` → 设信标主/副效果并扣支付物（等价信标 GUI"点效果图标+点√"，直接发 `ServerboundSetBeaconPacket`）；params `primary`/`secondary` 为效果注册 id 如 `minecraft:haste`
- `container/select-trade` → 选中村民交易第 `index` 笔（等价点交易界面第 index 行：本地 `setSelectionHint` + 发 `ServerboundSelectTradePacket`，服务端 `tryMoveItems` 把付款物搬进支付格填到满叠）；只选中不消耗，交易仍由 `container/slot {slot:2}` 完成；返回 `{status,index,result}`（语义见 `村民交易API设计方案.md`）

### 1.3 视觉系统（v2，GPU 深度缓冲驱动，10 个类）

**管线**：`VisionApi.snapshot` → `DepthCapture.requestCapture()`（等待一帧，超时 15s）→ `Unprojector.visibleBlockHits()` 全量反投影 + `MemoryCellsReader.read()` 反向通道 → 主线程 `ObjectResolver.resolve()`（四路查询 + DeletionJudge 减量判定 + v2.31 群系 cell 采样 + 各 Store 落盘）→ 组装 JSON。

- **DepthCapture（深度图采集）**：vanilla 只支持 color 附件回读，故手写 GL——PBO 非阻塞回读 + fence 异步映射，**零卡顿、一帧完成、按需触发**。两个锚点：主深度（`LevelRendererMixin.renderLevel` TAIL）+ translucent 深度（`ChunkSectionsToRenderMixin.renderGroup(TRANSLUCENT)` TAIL，仅 Fabulous）。同帧握手保证深度与实体快照同步；late_debug gizmo 前哨跳过异常帧。
- **Unprojector（像素→世界坐标）**：纯数学。`inversePv` 反投影每像素（跳过天空/近裁剪），产出方块去重点（ε=0.05）+ 实体候选表面点；含 `projectToScreen` 世界→屏幕投影。
- **ObjectResolver（可见对象解析，四路查询）**：① 方块直查 + air 近侧回退（含实体相交验证）；② 方块实体搭方块便车（壁挂朝向过滤）；③ 实体正向像素归属（SectionPos 桶粗过滤 + slab 求交 + 肢体判别 A/B）；③.5 半透明掉落物（Fabulous）；④ 半透明/绊线方块（translucent 深度逐像素分类 + voxel DDA 区间射线推进 + 降级回退）。
- **DeletionJudge（删除判定，v2.23）**：几何证明式——实心不透明格存在时射线必在其近表面终止；"像素深度 ≥ 格远面距离"证明该格已消失。越过计数 ≥ 阈值（默认 2，由 cells 文件头下发）判消失。
- **MemoryCellsReader（反向通道）**：读记忆侧 `memory_cells.bin`（magic "SCEL"，小端），mtime 门控；文件缺失优雅降级为"只增不删"。v2.32：解析 **version=2** 头部（version 后带 UTF-8 维 id）→ `CellsData.dimension`；删除判定只对 `dimension == 本快照维` 的 cells 执行（version=1 旧文件无维标签 → 删除证据置空，宁缺勿滥）。
- **Store 落盘（gzip NBT，`<gameDir>/stevex/vision/`；v2.32 起按维分桶）**：文件顶层统一
  `{ "currentDimension": "<dimId>", "worlds": { "<dimId>": { 本维正文 }, … } }`——快照型只覆写当前维桶、
  其余维桶保留（换维不再清掉上一维"最后可见快照"）；累积型在各自维桶内 union（主/下界同坐标永不互撞）；
  读侧（记忆端）无 `worlds` 键 → 旧单维文件自动视为 overworld 桶。
  - `terrain.nbt`：**每次快照覆写当前维桶**，`blocks` + `deletions`（v2.23）。
  - `entities.nbt`：**覆写当前维桶**，轻量物理态 8 字段（id/type/pos/motion/rotation/onGround/health + uuid 键）**外加三类复原必需载荷**：掉落物 `item`（整份物品栈 tag，v2.34）、展示实体 `nbt`（整份可装载 payload，v2.35）、活体 `living`（全量属性，v2.41）。**不存每实体全量 NBT**（按需直读会引发 GC 风暴）；v2.40 起本文件同时是 `vision/entity` 的唯一数据源（不再直读活体，无 TTL/LRU 缓存）。
  - `block_entities.nbt`：**增量合并**（new/updated/skipped），全量 NBT；每维桶一份。
  - `biomes.nbt`（v2.31/v2.32）：**单调 union 累积覆写（当前维桶内）**，`cells`：cell key（quart 坐标 `"qx,qy,qz"`）→ biome id（如 `minecraft:plains`）；采样源 = 本帧可见方块集 + 相机 cell 锚点，**逐 cell、绝不逐方块**（同 cell 群系必然相同＝存储保证）；仅对不在 union 的 cell 解析，新增>0 才整体覆写。`snapshot` 的 `storeStats` 含 `biomes:{cells,added}`。
  - `containers.nbt`（v2.28/v2.30/v2.32）：会话化交互内容；`containers`（per-pos）分桶，**末影箱 `enderInventory`（玩家态）保持顶层全局**、跨维同一（真实 MC 末影箱语义）。
  - 各维桶自带 `agentPos/agentYaw/agentPitch/agentFov/dayTime`（姿态随桶存，每维一份）。
  - `snapshot` 响应顶层含 `"dimension"`（v2.32，与 store 的 `currentDimension` 一致）与 **`"effectsSource"`**（v2.42：`"integrated_server"` | `"unavailable"`——药水效果数据源的可用性，**进程级**事实；`unavailable` 时实体级 `effects` 键的缺席意味着"未知"而非"没有效果"）。
- **VisionCollector**：序列化中枢（实体整份 payload `serializeEntityFull` + `encodeItemStack` 物品栈编码助手）+ 数据层访问（四个 Store 单例）。v2.40 起**不再提供按需直读端点**（`collectEntityNbt` 已删）。

### 1.4 Mixins（10 个）
`KeyMappingAccessor`、`MinecraftAccessor`（F3 profiler）、`SoundEngineAccessor`/`SoundManagerAccessor`（声音）、`AbstractSignEditScreenAccessor`、`BookEditScreenAccessor`（均 @Accessor/@Invoker）、`LevelRendererMixin`（renderLevel/extractVisibleEntities/finalizeGizmoCollection 三处注入）、`ChunkSectionsToRenderMixin`（translucent 深度）。`ExampleMixin` 为空壳模板。

### 1.5 按键/输入实现
- `AgentInput`：封装 `KeyMapping.set/click`（经 KeyMappingAccessor 读 raw key），全部 `mc.execute()` 主线程执行。
- `AgentCamera`：增量 turn / lookAt。
- `AgentContainer`、`AgentText`：**空壳占位**——容器点击、文本输入逻辑内联在 ContainerApi/ChatApi 等 API 类中。

---

## 2. steveX 大脑（`src/`，Node ≥18，依赖仅 express + ws）

### 2.1 主入口 `main.js`
加载配置 → 创建 `AgentManager` → 启动 Web 服务器 → 处理 SIGINT 优雅退出。

### 2.2 `ModWSClient`（`src/mod/client.js`）
- 完整 JSON-RPC 客户端：请求/响应按 id 配对、超时、心跳无、**指数退避断线重连**（2s→30s）、status 缓存 + `refreshStatus()`。
- `call()` 永不抛异常，返回 `{ok,data}` / `{ok:false,error}`。
- 事件：`connected` / `disconnected` / `status`。
- `src/mod/index.js`：Phase A **单例连接**（getModClient），透传路由共享同一实例。

### 2.3 方法清单 `src/mod/methods.js`
51 个方法的元数据（method/category/description/zh/paramDefs），其中 `paramDefs` 是权威参数 schema
（每项 `name/type/required?/def/hint/sample`，已按 mod Java 源码逐条核实，含此前被误标为 `{}` 的
`container/slot`、`chat/send`、`sign/set`、`book/set` 等真实带参方法）。
`zh` 为查询器逐方法中文说明：**带参方法在文内直接枚举 `params` 可填键**（`?` 标可选，如
`container/slot` →「params 填 { slot, button?, clickType? }」、`vision/entity` →「params 填 { uuid(必填) }」），
**无参方法注明「无参数」**；`sample` = 查询器预填 step 模板的示例值（可与 `def` 不同以示可改，如 `camera/turn` 的 `dx` def 0 / sample 90）。
`METHOD_NAMES` 与 `methodsByCategory()` 供前端与状态页复用。`/api/skills` 的 `args` 由 `paramDefs` 派生成 name→type 紧凑对象。

### 2.4 `AgentManager` / `SteveXAgent`（`src/agent/`）
- 每个 agent = 一条到采集端 mod 的 WS 连接（`steveX_改进方案.md` §六 落地）。
- 生命周期：`connectAgent`/`disconnectAgent`/`reload`/`disconnectAll`，事件总线 `eventBus`（agent:connect/update/disconnect）。
- **状态缓存**：每 3s 轮询 `player`+`f3`+`status` 刷新 vitals/position/busy；前端实时展示。
- `getStatus()` 聚合 agent 状态（health/position/currentAction/mod{host,port,connected,busy,error}/model）。

### 2.5 mod 透传路由（`src/web/server/mod_routes.js`）
- `POST /api/mod/*splat`（通配捕获含斜杠方法如 `vision/snapshot`）→ 直传 mod，成功 200 / 失败统一 **502**。
- `GET /api/mod/status`（实时 refreshStatus + connected + 方法清单）、`POST /api/mod`（通用 `{method,params}` 入口）、`GET /api/mod/methods`。
- **批量时序 API**（`src/web/server/mod_batch.js`，设计见 `docs/批量时序API设计方案.md`）：
  `POST /api/mod/batch`（有序 step JSON + 步前 `waitMs`，后台串行执行，立即回 `batchId`）、
  `GET /api/mod/batch/:id`（每步 state/result 进度）、`POST /api/mod/batch/:id/stop`（中止）。
  全局单槽（并发 409 + runningId）、`stopOnError`、结束/中止自动补发 `pressed:false` 松键（`src/mod/pressable_methods.js`）。
  三条 batch 路由注册在 `*splat` 通配**之前**（否则被当地图方法名透传）。

### 2.6 Web 管理面板（`src/web/server/` + `src/web/public/`）
- Express 8090，静态托管前端，JSON body 解析。
- HTTP API：`GET /api/status`、`GET/POST /api/config/environment`（读/写 `configs/environments/app.json`，512KB 上限）、`POST /api/reload`、`POST /api/agents/:name/connect|disconnect`、`GET /api/skills`（51 方法别名）、404 catch-all。
- WS 广播（`ws.js`）：连接即发 snapshot；1s 节流脏标记广播；转发 eventBus 事件（agent:connect/disconnect/update 已接，**agent:llm:* 有接收端无触发源**）。
- **前端（SPA）**：8 个导航 tab，**Agents 与 Configs 已实现**，其余 6 个占位（alert Coming soon）。
  - Agents 页：agent 卡片（health 条/位置/model/mod 连接态）、**Mod API 查询器**（参考/助写，只读**不执行**，GET /api/mod/methods 填充）——搜索 + 按分类分组的方法下拉，选中后展开 detail：**顶部「语法框」**（`.modapi-grammar`）第一行为该方法的 pill+方法名+中文说明（`.modapi-meta` 内 `zh`，随选中刷新、文内枚举 `params` 可填键），其下为静态 step JSON 写法（`.modapi-grammar-code`），两行虚线分隔；再往下是可编辑 step 模板（预填 `paramDefs.sample` 示例值，无参为 `{"params":{}}`）与逐参数提示行（无参显示「无参数」）；按钮「追加到 Mod Batch」（JSON 校验后并入同卡 batch 文本框）/「复制」。）、搜索/过滤/排序、Agent Log 弹窗（cmd/llm 日志实时追加）、DOM diff 增量渲染。
  - **Mod Batch 面板**（批量时序，替代旧 `delay:` 前缀）：每行一个 step JSON（调用步 `{method,params,waitMs?}` / 纯等待步 `{waitMs}`）→ `POST /api/mod/batch`，~350ms 轮询 `GET /api/mod/batch/:id` 全量重绘进度，Stop 走 stop 接口；按键兜底由服务端负责，浏览器不再维护按键状态。
  - Configs 页：加载/格式化/复制/保存/保存并重载环境配置。
  - `lib/`：api.js（HTTP 封装）、ws-client.js（同源 WS，断线整页刷新）、state.js（响应式状态，日志上限 200 条）、icons.js（15 个内联 SVG）、utils.js（escapeHtml/truncate）。

### 2.7 LLM 客户端 `src/llm/deepseek_client.js`
`DeepSeekClient.chat(messages)`：调 DeepSeek chat/completions（默认 `deepseek-v4-flash`，20s 超时，AbortController）。**仅模块定义，未被任何 agent 循环引用——LLM 决策层尚未接线**。

### 2.8 占位目录
`src/benchmarks`、`src/blackboard`、`src/capsules`、`src/memory` 均为空 `.gitkeep`（子系统未实现）。

---

## 3. 记忆端 mod（`vendor/stevex-test-template-1.21.11`）

Fabric 1.21.11 客户端 mod。核心设计：**文件驱动、mtime 门控，空闲成本≈0**。

### 3.1 整体工作流程
- 客户端启动自动创建/打开**纯虚空超平坦单人世界**（`MemoryWorld`，创造+和平+作弊，`resetOnLaunch` 可重建）。主世界始终虚空；`voidNetherEnd`（默认 true，v2.30）时下界/末地也建成纯虚空（保留各自维度类型/生物群系，仅不生成方块与结构）——生成器只在世界创建时定型，改配置需删除存档或 `resetOnLaunch=true` 重建。
- 每 tick 按序驱动：`TERRAIN.tick`（地形增量）→ `ENTITY.tick`（实体增量）→ `RESTORER.tick`（方块实体增量）→ `CONTAINER.tick`（容器内容对账，v2.28）→ `BIOME.tick`（生物群系复原，v2.31）→ `DELETION.apply`（减量）→ `CELLS.tick`（反向通道写 cells）→ 传送玩家到 agent 视角 + 同步 FOV。
- **v2.32（镜像跟随）**：上述序列每 tick 只驱动**活动维**（= agent 当前维，terrain.nbt 顶层 `currentDimension` 为权威源）的 ServerLevel；记忆玩家随之被传送到同一维，各复原器**只 apply 该维自己的维桶**、内容按维隔离（坐标永不跨维碰撞）。换维时先铺内容、管线底部再跨维传送玩家；未进入的维内容在文件里保留、进入即铺。
- **数据源自动探测**：配置显式路径 → `gameDir/stevex/vision/*.nbt` → **同级兄弟项目** `gameDir/../.. /stevex-template-1.21.11/run/stevex/vision/*.nbt` → 上级嵌套 → 兜底 `gameDir/config/stevex-test/*.nbt`。
- **mtime 门控**（v2.13）：每 `pollIntervalTicks`（默认 1）stat；mtime 未变不读不解压；仅成功读取后推进；半截写保留旧 mtime 重试。第二层**内容指纹门控**（表指纹变化才 sync）。
- 文件缺失不报错、挂起重试（30 tick 告警一次），出现后自动恢复。

### 3.2 `MemoryConfig`
`config/stevex-test/memory.json`（首次自动生成），16 项：worldName / sourceFile / terrainFile / entityFile / pollIntervalTicks / autoOpenOnLaunch / resetOnLaunch / **voidNetherEnd**(=true，下界/末地同主世界纯虚空，v2.30) / **removalEnabled** / **removalPixelThreshold**(=2，随 cells 下发) / removalMaxRayDist(=96) / memoryCellsWriteIntervalTicks / memoryCellsFile / **containerFile** / **containerReconcileOnPoll**(=true，每轮对账，v2.28) / **biomeFile**(v2.31)。

### 3.3 `MemoryWorldManager`
世界创建/打开/删除、`isMemoryWorld`（按世界名判定）、服务器切换清状态、首个玩家 setupPlayer（创造+飞行防掉虚空）、冻结 gamerule（`random_tick_speed=0`、`advance_time=false`、`advance_weather=false`、**`spawn_mobs=false`**，v2.30 §7.9 ⑧——建档时随 LevelSettings 写入 + 首次进入再设，双落点关闭生物自然生成）、`teleportToPose`（脚部-眼高、双精度、NaN 沿用当前朝向）、渲染线程同步基础 FOV。命令 `/memrestore`（GAMEMASTERS）强制重读。

- **v2.32（§5 镜像路由 + 跨维传送）**：新增 `DIM_BY_ID`（vanilla 三维 id → `Level.OVERWORLD/NETHER/END`）白名单、`lastAppliedPose`、`unsupportedDimWarnCooldown`。`onServerTick` 每 tick 以 `TERRAIN.activeDimensionId()`（terrain.nbt 顶层 `currentDimension` 最近一次成功读取值）为活动维权威源，直取 `server.getLevel(key)` 驱动；agent 在未知自定义维 → 300 tick 节流 WARN + 降级驱动玩家当前所在维（内容冻结→复原器近乎 no-op），回 vanilla 维自动恢复；无玩家 → overworld 兜底。gamerule 是**世界级**状态、三唯共享一份存档级规则，冻结设一次即全维生效。复原管线全部跑在活动维 level 后，底部以该维桶 agent 姿态 `teleportToPose(player, level, pose)` 传送玩家（level 是另一维即经 `TeleportTransition` changeDimension 完成换维）；**门控 = 跨维 ‖ 姿态变化**（玩家所在维 ≠ 活动维时无条件传送，同值跨维也须换，防困旧维）。

### 3.4 `TerrainRestorer` + `BlockStateUtil`
- 解析 terrain.nbt：`blocks` 表 + v2.23 新增 `deletions` + `agentPos`（旧文件缺键向前兼容）。
- 静默放置（flags 818 = UPDATE_CLIENTS | SKIP_ALL_SIDEEFFECTS）：不触发邻居更新、无掉落物、无 onPlace 副作用；**只放方块状态不挂 BE**（BE 由 MemoryRestorer 管），防 ghosting 清 stale。
- **纯累积**（只增不删，删除走几何证据路径）。
- `BlockStateUtil`：块 ID 重建 + 属性按序列化名/枚举常量名双重匹配兜底；`isSolidOpaque`（实心+不透明 = 唯一可删对象）。
- v2.32：读按维分桶文件（暴露 `activeDimensionId()` = 顶层 `currentDimension`，镜像活动维权威源）；`applied`/`appliedFingerprint` 按维隔离、mtime 门控 + 读取代际 × 每维一次交付，只 apply `level.dimension()` 维桶。

### 3.5 `EntityRestorer`
解析 entities.nbt（key=uuid）→ 按文件 uuid 强制生成 → 放置 → 移动（差>1e-4 teleport）→ **冻结**（NoGravity/Invulnerable/零速度/Mob NoAI+persistence/Item 无限寿命/登记 FROZEN 集合）。纯累积；减量接口 `allCellsEmpty`/`discard`。
- v2.32：已放置实体 / uuid 表按维隔离（chunk long / uuid 不编码维），`entities(dim)`/`uuids(dim)`/`currentUuids(dim)` 按维取；冻结只对活动维实体判定。
- v2.35（内容复原）：条目带 `nbt`（展示实体整份 payload）时按 payload 整份装载（`construct` 内 payload-first，失败回退默认构造）；payload 变化（`payloadChanged`）→ **整份重建**。
- v2.41（活体属性）：条目带 `living` 时 `applyLiving` 应用装备（8 槽 `setItemSlot`）/ 名字牌（Component 解码 + `setCustomName[Visible]`）/ 药水效果（`MobEffectInstance.CODEC` 解码 + 先清后加）/ 幼年（**instanceof 阶梯**——`Mob.setBaby` 是空实现，不可兜底）/ 姿态 / 着火。新建重建时在 `construct` 内应用，已存在实体属性变化时**就地重应用**（`move` 内，不重建。刻意不并进 `payloadChanged`：姿态/着火是快变状态）。冻结不 tick ⇒ 一次应用即永久保持（效果不倒计时、火不熄灭）。**`living` 键对每个 `LivingEntity` 恒写**（空时为 `{}`）——键的存在性只编码"是不是活体"；据此 `applyLiving` 走**全量对齐**（子键缺席即回缺省），否则"属性从有变无"（效果过期 / 装备被拿走 / 火灭了）与"本来就没有"无法区分，会留下镜像假事实。
  - v2.42 起效果段**已生效**（采集端数据源接通，见 v2.42 补记）：单机采集的条目会带 `effects` 子键，复原体身上真有药水效果；连真实服务器采集的条目仍不带（数据源不可用），此时 `removeAllEffects()` 对新建实体是空操作。**记忆端本文件零改动**——这正是 v2.41"保留实现"的兑现。

### 3.6 `MemoryRestorer`（方块实体通道）
增量合并 BE，静默放置，agent 视角姿态返回。
- v2.32：BE entries/applied/指纹按维隔离；`tick` 只返回/应用 `level.dimension()` 维桶，姿态（AgentPose）只读该维桶自带 agentPos/朝向/FOV；仅在新内容代际 × 首次驱动该维时返回 pose。

### 3.7 `DeletionApplier`（减量执行器，v2.23 / v2.36）
删除判定**完全在采集侧**（DeletionJudge），本类只执行。顺序**先增量后减量**（防重建）；相机格快路径（仍只删实心不透明）；**假阳性防护**（当前可见集不可删）；v2.36 起 deletion 主循环守卫放宽到 `isDeletableContent`（实心不透明 ∪ 流体（水/岩浆）∪ 满格透明，清 BE）；冻结实体清理需全部 AABB 占用格证空。
- v2.32：`apply` 只对传入的活动维 level 执行——删块/清 BE/实体清理全部限定该维（采集侧 cells 另带维标签双保险，镜像落后时宁缺勿滥）。

### 3.8 `MemoryCellReporter`（反向通道）
每 tick 把记忆世界"当前存在的实心不透明块 + 冻结实体占用格"按距离球过滤，写 `memory_cells.bin`（小端 SCEL；v2.32 起 **version=2**：magic/version/UTF-8 维 id 长度 L/L 字节 dimensionId/threshold/maxRayDist/count/count×BlockPos long），内容指纹门控 + 原子写（tmp + ATOMIC_MOVE）。只上报**活动维**内容并带维标签（采集侧据此只对同维 cells 判删）；活动维尚无玩家（跨维传送过渡 tick）跳过本轮且不推进指纹；`removalEnabled=false` 时纯累积不写。

### 3.9 Mixins（4 个）
- `LevelMixin`（`Level.tickBlockEntities` @HEAD cancellable）：记忆世界**取消全部方块实体 tick**（熔炉/漏斗/刷怪笼/活塞动画冻结在初始态）。
- `ServerLevelMixin`：`tickBlock`（冻结红石/活塞/TNT/火焰/重力方块）、`tickFluid`（冻结流体流动）、`tickNonPassenger`（仅对 FROZEN 实体取消 tick，拦截 TNT 引信/箭矢寿命等分发层计时器）。
- `PortalTravelMixin`（v2.33，`Entity.canUsePortal(boolean)` HEAD cancellable）：记忆世界里传送门旅行**统一闸门**置 false——下界门/末地门/末地折跃门 `entityInside` 不登记进内、`handlePortal` 不放行旅行 → `getPortalDestination`（`PortalForcer.createPortal` 新建出口门 / `EndPlatformFeature` 铺末地平台 / credits）永不执行；客户端 `animateTick`（粒子/声音）照常 → 门=纯装饰。打在基类 `Entity.canUsePortal`，LivingEntity 等覆写走 `super` 链必被强制 false。镜像跨维摆放用 `teleportTo`→`Entity.teleport`，不经此方法 → 不受影响。
- `ExampleMixin`：空壳。

### 3.10 `BiomeRestorer`（生物群系复原，v2.31）
- 读采集侧独立 `biomes.nbt`（`{timestamp, cells:{"qx,qy,qz":biomeId}}`，cell = 4×4×4 quart cell）；mtime 门控 + `prevIds` 快照 diff 出新增/变化 cell → 归组所在 chunk（`qx>>2, qz>>2`）入 `pendingChunks`。
- 逐 cell 语义：同 cell 所有方块 `getBiome` 必然相同（游戏存储保证）→ 只按 cell 填、绝不逐方块；cell key 用文件同款字符串，无跨端二进制打包约定。
- 写入**完全复用 vanilla `/fillbiome` 公开范式，零 Mixin / 零新包**：`chunk.fillBiomesFromNoise(resolver, randomState().sampler())`（resolver 对记录 cell 返回记录 holder，未记录返回 `chunk.getNoiseBiome` 现状 → 只覆盖不污染）→ `markUnsaved()`（落盘持久，块 unload→reload 从盘带回正确值）→ `chunkMap.resendBiomesForChunks(List.of(chunk))`（`ClientboundChunksBiomesPacket`，客户端即时重着色）。
- 生命周期：`MemoryWorldManager` 静态 `BIOME`，`onServerStart` 清空状态 → 首 tick 全量重读重填（支持 `resetOnLaunch` 重建）；tick 在 `CONTAINER` 后、`DELETION` 前。补填走 **tick-scan**（每 tick 扫 `pendingChunks` + `getChunkNow` 判加载，未加载块留待随传送加载后 ≤1 tick 补填），**不注册 CHUNK_LOAD 事件 → `StevexTest.java` 零改动**；限量 64 块/tick 防卡顿。注册表查不到 biome id → WARN 一次跳过。命令 `/memrestore` 一并 `forceRefresh`。
- v2.32：`cells/prevIds/pending` **按维分桶**（`pendingByDim`——chunk long 不编码维，主/下界同 chunk 坐标必须分桶防撞）；每 tick 只对 `level.dimension()` 维桶 diff/apply，群系永不跨维写入。

### 3.11 限制
仅支持单人内置服务器（`isDedicatedServer` 直接 return）；减量通道依赖采集端同版本配合（无握手/版本协商）。
- v2.32：镜像只还原 vanilla 三维（overworld/the_nether/the_end）；agent 在其它自定义维 → 节流告警暂停、数据在文件保留，回 vanilla 维自动恢复。已应用内容内存按维保留 + 区块落盘持久，镜像跨维切换不丢。

---

## 4. 脚本与工具

| 文件 | 已实现 |
|---|---|
| `scripts/bootstrap.ps1` | 校验 JDK 21+（解析版本号）、node_modules 缺失时 npm install、打印启动指引 |
| `scripts/start-all.ps1` | 一键拉起 3 进程（各开独立窗口）：采集端 gradlew runClient → npm start → 记忆端 gradlew runClient |
| `scripts/run-capture.ps1` | 单起采集端 `gradlew.bat runClient` |
| `scripts/run-memory.ps1` | 单起记忆端 `gradlew.bat runClient` |
| `test_mod_api.js` | 自实现 JSON-RPC 客户端，冒烟测试（status/player/f3/tablist/sound/profiler/settings/camera/hotbar/未知方法）；`--stevex` 测 HTTP 透传层；`--all` 测 vision/snapshot + busy 翻转；PASS/FAIL + 退出码 |
| `control_panel.html`（根目录与 steveX 下各一份，同内容） | 浏览器 WS 直连 25550 调试面板：实时坐标、批量 JSON 调用（delay: 语法）、51 方法 API 参考、日志、vision 40s 超时 |

---

## 5. 配置系统

- `configs/defaults/app.json`：默认单 agent `steveX`（mod 127.0.0.1:25550，llm deepseek-v4-flash，web 8090）。
- `configs/environments/app.json`：环境覆盖（当前配置 2 个 agent steveX-1/25550、steveX-2/25551，**llm.apiKey 为空**）。
- `loadConfig()` 深合并 defaults + environments；可通过 Web 面板读写并 `/api/reload` 热重载。

---

## 6. 端口与依赖

| 项 | 值 |
|---|---|
| Web 面板 / REST | **8090** |
| 采集端 mod WS | **25550**（写死 mod 内） |
| Node | ≥18（本机 v24） |
| JDK | 21+（本机 25） |
| 首次构建 | 需联网下载 Gradle 9.5.1 + Fabric 依赖 + Minecraft 客户端 jar |

---

## 7. 未实现 / 占位 / 遗留汇总

| 项 | 状态 |
|---|---|
| `AgentContainer.java`、`AgentText.java` | 空壳占位（逻辑内联在各 API 类） |
| 两个 mod 的 `ExampleMixin` | 空壳模板 |
| `SteveX.java` | 无初始化逻辑（仅日志） |
| Web 面板 6/8 个 tab（Overview/World/Tasks/Capsules/Evolution/Logs） | 未实现（Coming soon） |
| **LLM 决策层** | `DeepSeekClient` 已定义未接线；`llm.apiKey` 空；`agent:llm:*` 事件无触发源；无 agent 行为循环 |
| `src/benchmarks`、`blackboard`、`capsules`、`memory` | 空占位目录 |
| 多 agent 每连接一实例（Phase B） | 未落地，当前单例连接（Phase A） |
| 记忆端专用服务器支持 | 未实现（仅单人内置服务器） |
| 减量通道跨端版本协商 | 无（单向协议，靠同版本配合） |
| mineflayer 命令层（旧 77 命令） | 已删除退役 |
